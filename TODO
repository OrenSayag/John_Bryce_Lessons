DONE
create components + inject ser
create ser
build ser:
  - tasks arr[] (with interface) {
    id: number,
    txt: string,
    completed: bool
  }
  - foo add (txt)
  - foo del (id)
  - foo edit (id)
build components
  - app
      form,
      list
  - form:
      div
        input
        submit btn, (click) add
  - list:
      item *ngFor [task]='task'
  - item:
      declare @Input()task


TODOS
      div
        div(txt)
        cb(comp), (click) comp
        btn(del), (click) del
cssing